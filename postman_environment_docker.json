{
  "name": "gRPC Inventory – Docker",
  "values": [
    {
      "key": "base_url",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true,
      "description": "REST API – Docker exposes port 8080 to the host"
    },
    {
      "key": "grpc_host",
      "value": "localhost:50051",
      "type": "default",
      "enabled": true,
      "description": "gRPC server – Docker exposes port 50051 to the host"
    }
  ],
  "_postman_variable_scope": "environment",
  "info": {
    "name": "gRPC Inventory – Docker",
    "description": "Run `docker compose up --build` before using this environment. Both services expose their ports to the host."
  }
}
