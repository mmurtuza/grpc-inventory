{
  "name": "gRPC Inventory – Local",
  "values": [
    {
      "key": "base_url",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true,
      "description": "REST API base URL (direct Go server)"
    },
    {
      "key": "grpc_host",
      "value": "localhost:50051",
      "type": "default",
      "enabled": true,
      "description": "gRPC server host:port"
    }
  ],
  "_postman_variable_scope": "environment",
  "info": {
    "name": "gRPC Inventory – Local",
    "description": "Run `go run ./rest-api` and `go run ./server` locally before using this environment."
  }
}
